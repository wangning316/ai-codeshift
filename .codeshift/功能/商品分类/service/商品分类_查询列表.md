## 商品分类(service)

## 方法描述
查询商品分类列表

## 数据库操作
可同时根据`查询商品分类-请求参数（Query）`传入的所有有值的参数使用`商品分类(mapper)`的数据库操作对象根据`分类名称`、`开启状态`、`父分类编号`、`父分类编号数组`查询`商品分类(entity)`列表。

## 数据转换
将数据库查询的`商品分类(entity)`列表，转换为`查询商品分类列表-响应参数`数据对象。

## 返回数据
返回`查询商品分类列表-响应参数`数据对象。